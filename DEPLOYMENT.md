# Version 11.0 Deployment

1. Upload and replace all project files with this package.
2. Commit with `Add Version 11 quote integrity validation`.
3. Wait for GitHub Pages deployment success.
4. Keep `mode=live` and `showLogs=true`; change the cache parameter to `v=110`.
5. Publish the cTrader placement, close previous instances and reopen one instance.
6. Verify build `11.0-quote-integrity`.
