# Version 13.0.1 Deployment

1. Replace all repository files with this package.
2. Commit with `Fix adaptive cTrader quote representation`.
3. Wait for GitHub Pages deployment success.
4. Keep `mode=live` and `showLogs=true`; set the cache parameter to `v=1301`.
5. Publish the cTrader placement, close all old instances, and reopen one instance.
6. Verify build `13.0.1-adaptive-quote-final`.
