export const probePresets = Object.freeze([]);
export async function runServerDataProbe() {
  return {
    ok: false,
    error: "Development server-data probing is unavailable in Version 10.0."
  };
}
