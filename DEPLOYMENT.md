# Version 12.0 Deployment

1. Upload and replace all project files with this package.
2. Commit with `Release Version 12 auditable P/L monitoring`.
3. Wait for GitHub Pages deployment success.
4. Keep `mode=live` and `showLogs=true`; change the cache parameter to `v=120`.
5. Publish the cTrader placement, close old instances and reopen one instance.
6. Verify build `12.0-auditable-pnl`.
