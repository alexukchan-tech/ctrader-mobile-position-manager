# cTrader Mobile Position Manager 4.6 Interface Inspector

This strictly read-only build inspects the SDK's exported `ServerInterfaces` object for position, order, reconcile, account, execution, payload, server, and data definitions. It does not send a trade-management command. Export the result using **Copy Interface Report**.
